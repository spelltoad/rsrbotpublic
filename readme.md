# rsrbotpublic

Telegram bot to automate upload to ydisk from telegram. Misses `region_codes.py` file with group chats' ids for NDA reasons.

It utilizes a `yadisk` library to handle yandex disk api and create files and upload photos. `python-telegram-bot` library is used for telegram functionality.
